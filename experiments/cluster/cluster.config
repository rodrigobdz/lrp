[DEFAULT]
WORKSPACE_ROOT = /home/rodrigo
VERBOSE = True

[EXPERIMENTS]
MODEL = vgg16
EXP_NAME_SHORT = decr-gamma

[PLOTS]
TITLE = Contourf plot of AUC values across experiments
X_LABEL = Gammas for LRP-Gamma in VGG16 layers 1-17
Y_LABEL = Gammas for LRP-Gamma in VGG16 layers 18-24

[DATA]
BATCH_SIZE = 4
NUMBER_OF_BATCHES = 1
IMAGE_CLASSES = ["axolotl"]

[LRP]
NUMBER_OF_HYPERPARAMETER_VALUES = 4
TOTAL_NUMBER_OF_EXPERIMENTS = 16
SAMPLING_RANGE_START = 0.0001
SAMPLING_RANGE_END = 1

[PIXEL_FLIPPING]
PERTURBATION_STEPS = 29
PERTURBATION_SIZE = 8

[PATHS]
DATASET_ROOT = ${WORKSPACE_ROOT}/data
EXPERIMENT_PARENT_ROOT = ${WORKSPACE_ROOT}/experiment-results/2022-06-20/cluster/${EXPERIMENTS:MODEL}-${EXPERIMENTS:EXP_NAME_SHORT}-batch-size-${DATA:BATCH_SIZE}-total-experiments-${LRP:TOTAL_NUMBER_OF_EXPERIMENTS}
PLOT_ROOT = ${EXPERIMENT_PARENT_ROOT}/plot
PLOT_PATH = ${PLOT_ROOT}/lrp-hyperparam-search-plot.png
PLOT_X_VALUES_PATH = ${PLOT_ROOT}/x-values.npy
PLOT_Y_VALUES_PATH = ${PLOT_ROOT}/y-values.npy
PLOT_Z_VALUES_PATH = ${PLOT_ROOT}/z-values.npy
